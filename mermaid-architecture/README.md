# Mermaid Architecture

- **architecture-overview.md** — High-level diagram: one app, routers, and external services. Use for quick orientation.
- **architecture-detailed.md** — Detailed diagram: entry point, config, auth, each router, and downstream calls. Use for implementation and onboarding.

Render the `mermaid` code blocks in any Markdown viewer that supports Mermaid (e.g. GitHub, VS Code with extension, or [mermaid.live](https://mermaid.live)).
